---
applications:
  - name: my-joker-app       # 应用名称，自己起
    memory: 128M              # 这种应用通常 64M-128M 内存就够了
    disk_quota: 256M
    buildpack: nodejs_buildpack
    command: node index.js    # 确保这里的入口文件是 package.json 里定义的 main 文件
    env:
      UUID: "7fb287b9-643d-4cca-858c-3560df4a8a44" # 这种项目通常需要一个 UUID 作为鉴权密码
